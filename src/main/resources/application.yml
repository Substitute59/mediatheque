spring:
  datasource:
    url: ${JDBC_DATABASE_URL}
    username: ${JDBC_DATABASE_USERNAME}
    password: ${JDBC_DATABASE_PASSWORD}
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
        id:
          new_generator_mappings: true
  mail:
    host: bente.o2switch.net
    port: 587
    username: dev@fumi2936.odns.fr
    password: "D2,rs.m;Aw^T"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl.trust: "*"
          debug: true
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
error:
  handling:
    http-status-in-json-response: true
    exception-logging: NO_LOGGING
    full-stacktrace-http-statuses: 5xx
    log-levels:
      5xx: ERROR
jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000
frontend:
  url: ${FRONTEND_URL}
